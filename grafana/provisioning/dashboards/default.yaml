apiVersion: 1

providers:
  - name: "My Dashboards" # A uniquely identifiable name for the provider
    folder: "" # The folder where to place the dashboards
    type: file
    options:
      path:
        /var/lib/grafana/dashboards